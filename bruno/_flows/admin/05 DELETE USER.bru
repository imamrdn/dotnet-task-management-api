meta {
  name: 05 DELETE USER
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/api/users/{{flowUserId}}
  body: none
  auth: bearer
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{authToken}}
}
