meta {
  name: CATEGORY BY ID
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/categories/{{categoryId}}
  body: none
  auth: bearer
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{authToken}}
}
