meta {
  name: DELETE CATEGORY
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/api/categories/{{categoryId}}
  body: none
  auth: bearer
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{authToken}}
}
