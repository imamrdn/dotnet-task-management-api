meta {
  name: USER PROFILE
  type: http
  seq: 7
}

get {
  url: {{baseUrl}}/api/users/{{userId}}/profile
  body: none
  auth: bearer
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{authToken}}
}
