meta {
  name: DELETE TASK
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/api/tasks/{{taskId}}
  body: none
  auth: bearer
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{authToken}}
}
