meta {
  name: TASK LIST
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/api/tasks?page={{page}}&limit={{limit}}
  body: none
  auth: bearer
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{authToken}}
}
